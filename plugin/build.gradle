plugins {
    id 'java-gradle-plugin'
    id 'maven-publish'
}
group = 'by.harlap'
version = '1.0.0'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
}

gradlePlugin {
    plugins {
        gitplugin {
            id = 'by.harlap.gitplugin'
            implementationClass = 'by.harlap.plugin.GitPlugin'
        }
    }
}